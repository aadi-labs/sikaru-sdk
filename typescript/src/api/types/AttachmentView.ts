// This file was auto-generated by Fern from our API Definition.

import * as Sikaru from "../index.js";

export interface AttachmentView {
    capabilities: string[];
    cleanup_at: number | null;
    cleanup_status: AttachmentView.CleanupStatus;
    environment_id: string;
    id: string;
    journal_id: string;
    /** Renewal TTL; anchor only ready/heartbeat acknowledgments to request-start monotonic time. Status and poll responses never renew the lease. */
    lease_ttl_seconds?: number | undefined;
    lease_until: number;
    owner_epoch: number;
    owner_id: string | null;
    processes: Sikaru.ProcessObservation[];
    project_id: string;
    protocol_version: string | null;
    provider_id: string;
    session_id: string;
    startup_deadline: number;
    status: AttachmentView.Status;
    uncertain_operations: Sikaru.UncertainOperation[];
    workspace_generation: string;
    workspace_provenance: Sikaru.WorkspaceProvenance;
}

export namespace AttachmentView {
    export const CleanupStatus = {
            Unconfirmed: "unconfirmed",
            Confirmed: "confirmed"
        } as const;
    export type CleanupStatus = typeof CleanupStatus[keyof typeof CleanupStatus];
    export const Status = {
            Pending: "pending",
            Starting: "starting",
            Ready: "ready",
            Stopping: "stopping",
            Cleaned: "cleaned",
            StartupExpired: "startup_expired",
            RecoveryRequired: "recovery_required",
            Abandoned: "abandoned"
        } as const;
    export type Status = typeof Status[keyof typeof Status];
}
