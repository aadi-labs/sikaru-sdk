[package]
name = "compute-contract"
version = "0.0.0"
edition = "2021"
publish = false
[dependencies]
sikaru = { path = "../../../rust" }
tokio = { version = "1", features = ["macros", "rt-multi-thread"] }
serde_json = "1"
