# This file was auto-generated by Fern from our API Definition.

import typing

from ..core.client_wrapper import AsyncClientWrapper, SyncClientWrapper
from ..core.request_options import RequestOptions
from ..types.attachment_view import AttachmentView
from ..types.claim_view import ClaimView
from ..types.credential_issued import CredentialIssued
from ..types.process_observation import ProcessObservation
from ..types.ready_input_capabilities_item import ReadyInputCapabilitiesItem
from ..types.ready_input_protocol_version import ReadyInputProtocolVersion
from ..types.receipt_input import ReceiptInput
from ..types.reconcile_view import ReconcileView
from ..types.workspace_provenance import WorkspaceProvenance
from .raw_client import AsyncRawComputeAttachmentsClient, RawComputeAttachmentsClient

# this is used as the default value for optional parameters
OMIT = typing.cast(typing.Any, ...)


class ComputeAttachmentsClient:
    def __init__(self, *, client_wrapper: SyncClientWrapper):
        self._raw_client = RawComputeAttachmentsClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> RawComputeAttachmentsClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        RawComputeAttachmentsClient
        """
        return self._raw_client

    def get(
        self, project_id: str, attachment_id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> AttachmentView:
        """
        Parameters
        ----------
        project_id : str

        attachment_id : str

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AttachmentView
            Successful Response

        Examples
        --------
        from sikaru_api import SikaruApi

        client = SikaruApi(
            api_key="YOUR_API_KEY",
        )
        client.compute_attachments.get(
            project_id="project_id",
            attachment_id="attachment_id",
        )
        """
        _response = self._raw_client.get(project_id, attachment_id, request_options=request_options)
        return _response.data

    def abandon(
        self,
        project_id: str,
        attachment_id: str,
        *,
        evidence: str,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> AttachmentView:
        """
        Irreversibly abandon execution authority, preserving unknown effects. Cleanup is still required.

        Parameters
        ----------
        project_id : str

        attachment_id : str

        evidence : str

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AttachmentView
            Successful Response

        Examples
        --------
        from sikaru_api import SikaruApi

        client = SikaruApi(
            api_key="YOUR_API_KEY",
        )
        client.compute_attachments.abandon(
            project_id="project_id",
            attachment_id="attachment_id",
            evidence="evidence",
        )
        """
        _response = self._raw_client.abandon(
            project_id, attachment_id, evidence=evidence, request_options=request_options
        )
        return _response.data

    def cancel(
        self, project_id: str, attachment_id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> AttachmentView:
        """
        Idempotently request stopping. This never asserts child cleanup.

        Parameters
        ----------
        project_id : str

        attachment_id : str

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AttachmentView
            Successful Response

        Examples
        --------
        from sikaru_api import SikaruApi

        client = SikaruApi(
            api_key="YOUR_API_KEY",
        )
        client.compute_attachments.cancel(
            project_id="project_id",
            attachment_id="attachment_id",
        )
        """
        _response = self._raw_client.cancel(project_id, attachment_id, request_options=request_options)
        return _response.data

    def claim(
        self,
        project_id: str,
        attachment_id: str,
        *,
        idempotency_key: str,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> ClaimView:
        """
        Same attachment/key/worker returns the same launch identity, never another launch.

        Parameters
        ----------
        project_id : str

        attachment_id : str

        idempotency_key : str

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ClaimView
            Successful Response

        Examples
        --------
        from sikaru_api import SikaruApi

        client = SikaruApi(
            api_key="YOUR_API_KEY",
        )
        client.compute_attachments.claim(
            project_id="project_id",
            attachment_id="attachment_id",
            idempotency_key="idempotency_key",
        )
        """
        _response = self._raw_client.claim(
            project_id, attachment_id, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    def cleanup(
        self,
        project_id: str,
        attachment_id: str,
        *,
        children_terminated: bool,
        evidence: str,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> AttachmentView:
        """
        Parameters
        ----------
        project_id : str

        attachment_id : str

        children_terminated : bool

        evidence : str

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AttachmentView
            Successful Response

        Examples
        --------
        from sikaru_api import SikaruApi

        client = SikaruApi(
            api_key="YOUR_API_KEY",
        )
        client.compute_attachments.cleanup(
            project_id="project_id",
            attachment_id="attachment_id",
            children_terminated=True,
            evidence="evidence",
        )
        """
        _response = self._raw_client.cleanup(
            project_id,
            attachment_id,
            children_terminated=children_terminated,
            evidence=evidence,
            request_options=request_options,
        )
        return _response.data

    def connect(
        self,
        project_id: str,
        attachment_id: str,
        *,
        capabilities: typing.Sequence[ReadyInputCapabilitiesItem],
        executor_instance_id: str,
        journal_id: str,
        protocol_version: ReadyInputProtocolVersion,
        workspace_provenance: WorkspaceProvenance,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> AttachmentView:
        """
        Attest original workspace/journal. Reconnection never replays uncertain effects.

        Parameters
        ----------
        project_id : str

        attachment_id : str

        capabilities : typing.Sequence[ReadyInputCapabilitiesItem]
            Optional features beyond compute.execute. 'condition-waits-v1' serves bash.wait_for (WaitForArguments -> WaitForResult) and jobs.next_completed (NextCompletedArguments -> NextCompletedResult); only executors declaring it receive those operations.

        executor_instance_id : str

        journal_id : str

        protocol_version : ReadyInputProtocolVersion

        workspace_provenance : WorkspaceProvenance

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AttachmentView
            Successful Response

        Examples
        --------
        from sikaru_api import SikaruApi, WorkspaceProvenance

        client = SikaruApi(
            api_key="YOUR_API_KEY",
        )
        client.compute_attachments.connect(
            project_id="project_id",
            attachment_id="attachment_id",
            capabilities=["compute.execute"],
            executor_instance_id="executor_instance_id",
            journal_id="journal_id",
            protocol_version="sikaru-compute-v1",
            workspace_provenance=WorkspaceProvenance(
                identity="identity",
                kind="existing_directory",
            ),
        )
        """
        _response = self._raw_client.connect(
            project_id,
            attachment_id,
            capabilities=capabilities,
            executor_instance_id=executor_instance_id,
            journal_id=journal_id,
            protocol_version=protocol_version,
            workspace_provenance=workspace_provenance,
            request_options=request_options,
        )
        return _response.data

    def issue_credential(
        self,
        project_id: str,
        attachment_id: str,
        *,
        owner_epoch: int,
        owner_id: str,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> CredentialIssued:
        """
        Parameters
        ----------
        project_id : str

        attachment_id : str

        owner_epoch : int

        owner_id : str

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        CredentialIssued
            Successful Response

        Examples
        --------
        from sikaru_api import SikaruApi

        client = SikaruApi(
            api_key="YOUR_API_KEY",
        )
        client.compute_attachments.issue_credential(
            project_id="project_id",
            attachment_id="attachment_id",
            owner_epoch=1,
            owner_id="owner_id",
        )
        """
        _response = self._raw_client.issue_credential(
            project_id, attachment_id, owner_epoch=owner_epoch, owner_id=owner_id, request_options=request_options
        )
        return _response.data

    def heartbeat(
        self, project_id: str, attachment_id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> AttachmentView:
        """
        Parameters
        ----------
        project_id : str

        attachment_id : str

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AttachmentView
            Successful Response

        Examples
        --------
        from sikaru_api import SikaruApi

        client = SikaruApi(
            api_key="YOUR_API_KEY",
        )
        client.compute_attachments.heartbeat(
            project_id="project_id",
            attachment_id="attachment_id",
        )
        """
        _response = self._raw_client.heartbeat(project_id, attachment_id, request_options=request_options)
        return _response.data

    def ready(
        self,
        project_id: str,
        attachment_id: str,
        *,
        capabilities: typing.Sequence[ReadyInputCapabilitiesItem],
        executor_instance_id: str,
        journal_id: str,
        protocol_version: ReadyInputProtocolVersion,
        workspace_provenance: WorkspaceProvenance,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> AttachmentView:
        """
        Parameters
        ----------
        project_id : str

        attachment_id : str

        capabilities : typing.Sequence[ReadyInputCapabilitiesItem]
            Optional features beyond compute.execute. 'condition-waits-v1' serves bash.wait_for (WaitForArguments -> WaitForResult) and jobs.next_completed (NextCompletedArguments -> NextCompletedResult); only executors declaring it receive those operations.

        executor_instance_id : str

        journal_id : str

        protocol_version : ReadyInputProtocolVersion

        workspace_provenance : WorkspaceProvenance

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AttachmentView
            Successful Response

        Examples
        --------
        from sikaru_api import SikaruApi, WorkspaceProvenance

        client = SikaruApi(
            api_key="YOUR_API_KEY",
        )
        client.compute_attachments.ready(
            project_id="project_id",
            attachment_id="attachment_id",
            capabilities=["compute.execute"],
            executor_instance_id="executor_instance_id",
            journal_id="journal_id",
            protocol_version="sikaru-compute-v1",
            workspace_provenance=WorkspaceProvenance(
                identity="identity",
                kind="existing_directory",
            ),
        )
        """
        _response = self._raw_client.ready(
            project_id,
            attachment_id,
            capabilities=capabilities,
            executor_instance_id=executor_instance_id,
            journal_id=journal_id,
            protocol_version=protocol_version,
            workspace_provenance=workspace_provenance,
            request_options=request_options,
        )
        return _response.data

    def reconcile(
        self,
        project_id: str,
        attachment_id: str,
        *,
        executor_instance_id: str,
        journal_id: str,
        workspace_provenance: WorkspaceProvenance,
        processes: typing.Optional[typing.Sequence[ProcessObservation]] = OMIT,
        receipts: typing.Optional[typing.Sequence[ReceiptInput]] = OMIT,
        uncertain_operation_ids: typing.Optional[typing.Sequence[str]] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> ReconcileView:
        """
        Parameters
        ----------
        project_id : str

        attachment_id : str

        executor_instance_id : str

        journal_id : str

        workspace_provenance : WorkspaceProvenance

        processes : typing.Optional[typing.Sequence[ProcessObservation]]

        receipts : typing.Optional[typing.Sequence[ReceiptInput]]

        uncertain_operation_ids : typing.Optional[typing.Sequence[str]]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ReconcileView
            Successful Response

        Examples
        --------
        from sikaru_api import SikaruApi, WorkspaceProvenance

        client = SikaruApi(
            api_key="YOUR_API_KEY",
        )
        client.compute_attachments.reconcile(
            project_id="project_id",
            attachment_id="attachment_id",
            executor_instance_id="executor_instance_id",
            journal_id="journal_id",
            workspace_provenance=WorkspaceProvenance(
                identity="identity",
                kind="existing_directory",
            ),
        )
        """
        _response = self._raw_client.reconcile(
            project_id,
            attachment_id,
            executor_instance_id=executor_instance_id,
            journal_id=journal_id,
            workspace_provenance=workspace_provenance,
            processes=processes,
            receipts=receipts,
            uncertain_operation_ids=uncertain_operation_ids,
            request_options=request_options,
        )
        return _response.data

    def status(
        self, project_id: str, attachment_id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> AttachmentView:
        """
        Parameters
        ----------
        project_id : str

        attachment_id : str

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AttachmentView
            Successful Response

        Examples
        --------
        from sikaru_api import SikaruApi

        client = SikaruApi(
            api_key="YOUR_API_KEY",
        )
        client.compute_attachments.status(
            project_id="project_id",
            attachment_id="attachment_id",
        )
        """
        _response = self._raw_client.status(project_id, attachment_id, request_options=request_options)
        return _response.data

    def stop(
        self, project_id: str, attachment_id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> AttachmentView:
        """
        Executor can stop its own attachment and cancel its runs; cleanup is separately reported.

        Parameters
        ----------
        project_id : str

        attachment_id : str

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AttachmentView
            Successful Response

        Examples
        --------
        from sikaru_api import SikaruApi

        client = SikaruApi(
            api_key="YOUR_API_KEY",
        )
        client.compute_attachments.stop(
            project_id="project_id",
            attachment_id="attachment_id",
        )
        """
        _response = self._raw_client.stop(project_id, attachment_id, request_options=request_options)
        return _response.data

    def teardown(
        self,
        project_id: str,
        attachment_id: str,
        *,
        children_terminated: bool,
        evidence: str,
        owner_epoch: int,
        owner_id: str,
        workspace_generation: str,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> AttachmentView:
        """
        Controller or owning worker attests sandbox teardown; clean parked turns stay resumable.

        Parameters
        ----------
        project_id : str

        attachment_id : str

        children_terminated : bool

        evidence : str

        owner_epoch : int

        owner_id : str

        workspace_generation : str

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AttachmentView
            Successful Response

        Examples
        --------
        from sikaru_api import SikaruApi

        client = SikaruApi(
            api_key="YOUR_API_KEY",
        )
        client.compute_attachments.teardown(
            project_id="project_id",
            attachment_id="attachment_id",
            children_terminated=True,
            evidence="evidence",
            owner_epoch=1,
            owner_id="owner_id",
            workspace_generation="workspace_generation",
        )
        """
        _response = self._raw_client.teardown(
            project_id,
            attachment_id,
            children_terminated=children_terminated,
            evidence=evidence,
            owner_epoch=owner_epoch,
            owner_id=owner_id,
            workspace_generation=workspace_generation,
            request_options=request_options,
        )
        return _response.data

    def create(
        self,
        project_id: str,
        session_id: str,
        *,
        environment_id: str,
        idempotency_key: str,
        workspace_provenance: WorkspaceProvenance,
        replace_existing: typing.Optional[bool] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> AttachmentView:
        """
        Same session/key/workspace returns the original binding; changed workspace conflicts.

        Parameters
        ----------
        project_id : str

        session_id : str

        environment_id : str

        idempotency_key : str

        workspace_provenance : WorkspaceProvenance

        replace_existing : typing.Optional[bool]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AttachmentView
            Successful Response

        Examples
        --------
        from sikaru_api import SikaruApi, WorkspaceProvenance

        client = SikaruApi(
            api_key="YOUR_API_KEY",
        )
        client.compute_attachments.create(
            project_id="project_id",
            session_id="session_id",
            environment_id="environment_id",
            idempotency_key="idempotency_key",
            workspace_provenance=WorkspaceProvenance(
                identity="identity",
                kind="existing_directory",
            ),
        )
        """
        _response = self._raw_client.create(
            project_id,
            session_id,
            environment_id=environment_id,
            idempotency_key=idempotency_key,
            workspace_provenance=workspace_provenance,
            replace_existing=replace_existing,
            request_options=request_options,
        )
        return _response.data


class AsyncComputeAttachmentsClient:
    def __init__(self, *, client_wrapper: AsyncClientWrapper):
        self._raw_client = AsyncRawComputeAttachmentsClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> AsyncRawComputeAttachmentsClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        AsyncRawComputeAttachmentsClient
        """
        return self._raw_client

    async def get(
        self, project_id: str, attachment_id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> AttachmentView:
        """
        Parameters
        ----------
        project_id : str

        attachment_id : str

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AttachmentView
            Successful Response

        Examples
        --------
        import asyncio

        from sikaru_api import AsyncSikaruApi

        client = AsyncSikaruApi(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.compute_attachments.get(
                project_id="project_id",
                attachment_id="attachment_id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.get(project_id, attachment_id, request_options=request_options)
        return _response.data

    async def abandon(
        self,
        project_id: str,
        attachment_id: str,
        *,
        evidence: str,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> AttachmentView:
        """
        Irreversibly abandon execution authority, preserving unknown effects. Cleanup is still required.

        Parameters
        ----------
        project_id : str

        attachment_id : str

        evidence : str

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AttachmentView
            Successful Response

        Examples
        --------
        import asyncio

        from sikaru_api import AsyncSikaruApi

        client = AsyncSikaruApi(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.compute_attachments.abandon(
                project_id="project_id",
                attachment_id="attachment_id",
                evidence="evidence",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.abandon(
            project_id, attachment_id, evidence=evidence, request_options=request_options
        )
        return _response.data

    async def cancel(
        self, project_id: str, attachment_id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> AttachmentView:
        """
        Idempotently request stopping. This never asserts child cleanup.

        Parameters
        ----------
        project_id : str

        attachment_id : str

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AttachmentView
            Successful Response

        Examples
        --------
        import asyncio

        from sikaru_api import AsyncSikaruApi

        client = AsyncSikaruApi(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.compute_attachments.cancel(
                project_id="project_id",
                attachment_id="attachment_id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.cancel(project_id, attachment_id, request_options=request_options)
        return _response.data

    async def claim(
        self,
        project_id: str,
        attachment_id: str,
        *,
        idempotency_key: str,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> ClaimView:
        """
        Same attachment/key/worker returns the same launch identity, never another launch.

        Parameters
        ----------
        project_id : str

        attachment_id : str

        idempotency_key : str

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ClaimView
            Successful Response

        Examples
        --------
        import asyncio

        from sikaru_api import AsyncSikaruApi

        client = AsyncSikaruApi(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.compute_attachments.claim(
                project_id="project_id",
                attachment_id="attachment_id",
                idempotency_key="idempotency_key",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.claim(
            project_id, attachment_id, idempotency_key=idempotency_key, request_options=request_options
        )
        return _response.data

    async def cleanup(
        self,
        project_id: str,
        attachment_id: str,
        *,
        children_terminated: bool,
        evidence: str,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> AttachmentView:
        """
        Parameters
        ----------
        project_id : str

        attachment_id : str

        children_terminated : bool

        evidence : str

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AttachmentView
            Successful Response

        Examples
        --------
        import asyncio

        from sikaru_api import AsyncSikaruApi

        client = AsyncSikaruApi(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.compute_attachments.cleanup(
                project_id="project_id",
                attachment_id="attachment_id",
                children_terminated=True,
                evidence="evidence",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.cleanup(
            project_id,
            attachment_id,
            children_terminated=children_terminated,
            evidence=evidence,
            request_options=request_options,
        )
        return _response.data

    async def connect(
        self,
        project_id: str,
        attachment_id: str,
        *,
        capabilities: typing.Sequence[ReadyInputCapabilitiesItem],
        executor_instance_id: str,
        journal_id: str,
        protocol_version: ReadyInputProtocolVersion,
        workspace_provenance: WorkspaceProvenance,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> AttachmentView:
        """
        Attest original workspace/journal. Reconnection never replays uncertain effects.

        Parameters
        ----------
        project_id : str

        attachment_id : str

        capabilities : typing.Sequence[ReadyInputCapabilitiesItem]
            Optional features beyond compute.execute. 'condition-waits-v1' serves bash.wait_for (WaitForArguments -> WaitForResult) and jobs.next_completed (NextCompletedArguments -> NextCompletedResult); only executors declaring it receive those operations.

        executor_instance_id : str

        journal_id : str

        protocol_version : ReadyInputProtocolVersion

        workspace_provenance : WorkspaceProvenance

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AttachmentView
            Successful Response

        Examples
        --------
        import asyncio

        from sikaru_api import AsyncSikaruApi, WorkspaceProvenance

        client = AsyncSikaruApi(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.compute_attachments.connect(
                project_id="project_id",
                attachment_id="attachment_id",
                capabilities=["compute.execute"],
                executor_instance_id="executor_instance_id",
                journal_id="journal_id",
                protocol_version="sikaru-compute-v1",
                workspace_provenance=WorkspaceProvenance(
                    identity="identity",
                    kind="existing_directory",
                ),
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.connect(
            project_id,
            attachment_id,
            capabilities=capabilities,
            executor_instance_id=executor_instance_id,
            journal_id=journal_id,
            protocol_version=protocol_version,
            workspace_provenance=workspace_provenance,
            request_options=request_options,
        )
        return _response.data

    async def issue_credential(
        self,
        project_id: str,
        attachment_id: str,
        *,
        owner_epoch: int,
        owner_id: str,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> CredentialIssued:
        """
        Parameters
        ----------
        project_id : str

        attachment_id : str

        owner_epoch : int

        owner_id : str

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        CredentialIssued
            Successful Response

        Examples
        --------
        import asyncio

        from sikaru_api import AsyncSikaruApi

        client = AsyncSikaruApi(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.compute_attachments.issue_credential(
                project_id="project_id",
                attachment_id="attachment_id",
                owner_epoch=1,
                owner_id="owner_id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.issue_credential(
            project_id, attachment_id, owner_epoch=owner_epoch, owner_id=owner_id, request_options=request_options
        )
        return _response.data

    async def heartbeat(
        self, project_id: str, attachment_id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> AttachmentView:
        """
        Parameters
        ----------
        project_id : str

        attachment_id : str

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AttachmentView
            Successful Response

        Examples
        --------
        import asyncio

        from sikaru_api import AsyncSikaruApi

        client = AsyncSikaruApi(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.compute_attachments.heartbeat(
                project_id="project_id",
                attachment_id="attachment_id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.heartbeat(project_id, attachment_id, request_options=request_options)
        return _response.data

    async def ready(
        self,
        project_id: str,
        attachment_id: str,
        *,
        capabilities: typing.Sequence[ReadyInputCapabilitiesItem],
        executor_instance_id: str,
        journal_id: str,
        protocol_version: ReadyInputProtocolVersion,
        workspace_provenance: WorkspaceProvenance,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> AttachmentView:
        """
        Parameters
        ----------
        project_id : str

        attachment_id : str

        capabilities : typing.Sequence[ReadyInputCapabilitiesItem]
            Optional features beyond compute.execute. 'condition-waits-v1' serves bash.wait_for (WaitForArguments -> WaitForResult) and jobs.next_completed (NextCompletedArguments -> NextCompletedResult); only executors declaring it receive those operations.

        executor_instance_id : str

        journal_id : str

        protocol_version : ReadyInputProtocolVersion

        workspace_provenance : WorkspaceProvenance

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AttachmentView
            Successful Response

        Examples
        --------
        import asyncio

        from sikaru_api import AsyncSikaruApi, WorkspaceProvenance

        client = AsyncSikaruApi(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.compute_attachments.ready(
                project_id="project_id",
                attachment_id="attachment_id",
                capabilities=["compute.execute"],
                executor_instance_id="executor_instance_id",
                journal_id="journal_id",
                protocol_version="sikaru-compute-v1",
                workspace_provenance=WorkspaceProvenance(
                    identity="identity",
                    kind="existing_directory",
                ),
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.ready(
            project_id,
            attachment_id,
            capabilities=capabilities,
            executor_instance_id=executor_instance_id,
            journal_id=journal_id,
            protocol_version=protocol_version,
            workspace_provenance=workspace_provenance,
            request_options=request_options,
        )
        return _response.data

    async def reconcile(
        self,
        project_id: str,
        attachment_id: str,
        *,
        executor_instance_id: str,
        journal_id: str,
        workspace_provenance: WorkspaceProvenance,
        processes: typing.Optional[typing.Sequence[ProcessObservation]] = OMIT,
        receipts: typing.Optional[typing.Sequence[ReceiptInput]] = OMIT,
        uncertain_operation_ids: typing.Optional[typing.Sequence[str]] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> ReconcileView:
        """
        Parameters
        ----------
        project_id : str

        attachment_id : str

        executor_instance_id : str

        journal_id : str

        workspace_provenance : WorkspaceProvenance

        processes : typing.Optional[typing.Sequence[ProcessObservation]]

        receipts : typing.Optional[typing.Sequence[ReceiptInput]]

        uncertain_operation_ids : typing.Optional[typing.Sequence[str]]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ReconcileView
            Successful Response

        Examples
        --------
        import asyncio

        from sikaru_api import AsyncSikaruApi, WorkspaceProvenance

        client = AsyncSikaruApi(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.compute_attachments.reconcile(
                project_id="project_id",
                attachment_id="attachment_id",
                executor_instance_id="executor_instance_id",
                journal_id="journal_id",
                workspace_provenance=WorkspaceProvenance(
                    identity="identity",
                    kind="existing_directory",
                ),
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.reconcile(
            project_id,
            attachment_id,
            executor_instance_id=executor_instance_id,
            journal_id=journal_id,
            workspace_provenance=workspace_provenance,
            processes=processes,
            receipts=receipts,
            uncertain_operation_ids=uncertain_operation_ids,
            request_options=request_options,
        )
        return _response.data

    async def status(
        self, project_id: str, attachment_id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> AttachmentView:
        """
        Parameters
        ----------
        project_id : str

        attachment_id : str

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AttachmentView
            Successful Response

        Examples
        --------
        import asyncio

        from sikaru_api import AsyncSikaruApi

        client = AsyncSikaruApi(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.compute_attachments.status(
                project_id="project_id",
                attachment_id="attachment_id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.status(project_id, attachment_id, request_options=request_options)
        return _response.data

    async def stop(
        self, project_id: str, attachment_id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> AttachmentView:
        """
        Executor can stop its own attachment and cancel its runs; cleanup is separately reported.

        Parameters
        ----------
        project_id : str

        attachment_id : str

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AttachmentView
            Successful Response

        Examples
        --------
        import asyncio

        from sikaru_api import AsyncSikaruApi

        client = AsyncSikaruApi(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.compute_attachments.stop(
                project_id="project_id",
                attachment_id="attachment_id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.stop(project_id, attachment_id, request_options=request_options)
        return _response.data

    async def teardown(
        self,
        project_id: str,
        attachment_id: str,
        *,
        children_terminated: bool,
        evidence: str,
        owner_epoch: int,
        owner_id: str,
        workspace_generation: str,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> AttachmentView:
        """
        Controller or owning worker attests sandbox teardown; clean parked turns stay resumable.

        Parameters
        ----------
        project_id : str

        attachment_id : str

        children_terminated : bool

        evidence : str

        owner_epoch : int

        owner_id : str

        workspace_generation : str

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AttachmentView
            Successful Response

        Examples
        --------
        import asyncio

        from sikaru_api import AsyncSikaruApi

        client = AsyncSikaruApi(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.compute_attachments.teardown(
                project_id="project_id",
                attachment_id="attachment_id",
                children_terminated=True,
                evidence="evidence",
                owner_epoch=1,
                owner_id="owner_id",
                workspace_generation="workspace_generation",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.teardown(
            project_id,
            attachment_id,
            children_terminated=children_terminated,
            evidence=evidence,
            owner_epoch=owner_epoch,
            owner_id=owner_id,
            workspace_generation=workspace_generation,
            request_options=request_options,
        )
        return _response.data

    async def create(
        self,
        project_id: str,
        session_id: str,
        *,
        environment_id: str,
        idempotency_key: str,
        workspace_provenance: WorkspaceProvenance,
        replace_existing: typing.Optional[bool] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> AttachmentView:
        """
        Same session/key/workspace returns the original binding; changed workspace conflicts.

        Parameters
        ----------
        project_id : str

        session_id : str

        environment_id : str

        idempotency_key : str

        workspace_provenance : WorkspaceProvenance

        replace_existing : typing.Optional[bool]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AttachmentView
            Successful Response

        Examples
        --------
        import asyncio

        from sikaru_api import AsyncSikaruApi, WorkspaceProvenance

        client = AsyncSikaruApi(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.compute_attachments.create(
                project_id="project_id",
                session_id="session_id",
                environment_id="environment_id",
                idempotency_key="idempotency_key",
                workspace_provenance=WorkspaceProvenance(
                    identity="identity",
                    kind="existing_directory",
                ),
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.create(
            project_id,
            session_id,
            environment_id=environment_id,
            idempotency_key=idempotency_key,
            workspace_provenance=workspace_provenance,
            replace_existing=replace_existing,
            request_options=request_options,
        )
        return _response.data
